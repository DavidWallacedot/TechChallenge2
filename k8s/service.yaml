apiVersion: v1
kind: Service
metadata:
  name: techchallenge2-app-service
spec:
  type: LoadBalancer
  selector:
    app: techchallenge2-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
